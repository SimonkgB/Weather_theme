# Weather_theme

## Plan:

```bash
Grab data from wttr.in (given in JSON)
curl every ~30min
categorize the temp, weather etc
apply that categorized preseted theme which reflects that condition to the overall machine (waybar and terminal) ++ all apps using pywal and/or wpgtk

Cool extra: make the waybar with the different sections and colors light up ~white using position, accelerometer or something else to follow the moon based on pc orientation
```
